# Inno — Cleanup Plan

## Deferred

### 1. [DEFERRED] Systemd service has `--no-dbus` flag
- `inno.service:7` — disables control interface, contradicts README
- Fix after all bugs/stale code cleanup
- Also redo README at that time
